__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/

# third-party tool clones (rebuildable; see memory/docs)
external/

# single oversized LIGA log (128 MB, from the failed Fe run) — exceeds GitHub's
# 100 MB hard limit, so it can't be pushed. Everything else in data/liga/ is kept.
data/liga/Fe_mp-13_liga.log

.DS_Store
